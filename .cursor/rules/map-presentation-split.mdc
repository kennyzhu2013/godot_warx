---
description: 表现层脚本拆分尺度：避免过碎与上帝类
globs: scripts/map/presentation/**/*.gd,docs/architecture/LAYERED_ARCHITECTURE.md,docs/terrain/TERRAIN_TILES.md
alwaysApply: false
---

# 表现层拆分原则

总纲：`docs/architecture/LAYERED_ARCHITECTURE.md` §「脚本拆分尺度」。

## 何时拆

- **跨层**：Data / Catalog / Logic / Present / Editor 边界清晰时必须拆
- **复用**：≥2 个无关模块共用（如 `HeightfieldMesh` 供地面与水面）
- **生命周期不同**：Node（Layer）vs 纯算法 / Resource（材质 `.tres`）

## 何时不拆

- 仅转发一行的 `*Builder` / 薄封装
- 「为了目录整齐」把同一职责拆成 3+ 文件
- 把 Catalog 路径解析塞进 Layer 组网循环

## 地面管线（参照实现）

| 脚本 | 职责 |
| ------ | ------ |
| `HeightfieldMesh` | 采样 + Packed* 三角/四边形批建 + `active_material` / 调试 uniform |
| `MapTerrainLayer` | WC3 地表规则 + `@export ground_material` + 调底层画网格 |
| `MapDebugGridLayer` | 跨子系统调试栅格（地面/崖/水共用），不塞进 TerrainLayer |
| `assets/materials/*.tres` + `assets/shaders/*` | 静态材质与 shader；Layer 只 `duplicate` 后设运行时参数 |
| `Wc3GroundTileCatalog` | tileset → Texture2DArray |

## 反例

```gdscript
# ❌ 代码里 new ShaderMaterial + 写死 roughness；preload 路径写死在 Layer const
var mat := ShaderMaterial.new()
mat.shader = preload("...")

# ✅ 场景 @export 指向 assets/materials；Mesh 持有 active_material
_ground.apply_from_template(ground_material, {"tilesets": tex_array})
```
