# 基于 godot_war3 实现军团战争

> 目标：在 Godot 里做出可玩的《军团战争 TD》5.34c。地图和模型用 `godot_war3`，规则用已有的 LegionP0 模拟器。
> 数值以同目录 [军团战争TD_5.34c_策划案.md](军团战争TD_5.34c_策划案.md) 为准。出怪表、基础伐木量、击杀赏金在策划案里未还原，实现时写成命名常量，文件头标明「自定规格」。
> 本文只定实现边界和阶段，不改游戏代码。

---

## 1. 结论

可以做。`godot_war3` 负责地形、模型、镜头和受击表现。回合、经济、伤害、胜负只写在 LegionP0 的 `sim/` 里。3D 节点只播放快照，不调用 `DamagePipeline`，也不跑人族 Melee。

原图玩法在加密的 `war3map.bin` 里。`godot_war3` 当前没有 JASS / Lua 宿主，不能把 `.w3x` 丢进 `game_main.tscn` 指望它跑起来。它已经落地的对战是 Echo Isles 上的人族 Melee，和本图无关。

```text
策划案 + SLK + godot/data
        │
        ▼
LegionP0 sim（20Hz 确定性 tick）
        │  每拍快照
        ▼
view3d（只读快照，挂在 godot_war3 工程里）
        │
        ├── MapLoader.reload_from_hf     地形、悬崖、水
        └── add_unit_instance            按条目摆模型
```

人族模块不接入：`HarvestController`、`BuildController`、`MeleeBootstrap`、`AbilityBehaviorCatalog`、`unit_ai.gd`。它们和「准备回合造塔、雇怪打国王」抢同一套命令。

---

## 2. 工程怎么接

两套代码不在同一个 Godot 工程里。

| 角色 | 路径 | 渲染 |
|------|------|------|
| 规则脚本 | `d:\game2\rpg\mpqediten64\godot`（工程名 LegionP0） | GL Compatibility |
| 地形、模型、3D 视图宿主 | `d:\game2\rpg\mpqediten64\Work\godot_war3` | Forward Plus |
| 本文与策划案 | `Work\godot\` | 不进任何工程 |

`res://` 不能跨工程引用。魔兽模型场景依赖 Forward Plus，所以 **3D 视图放在 `godot_war3` 里**，把 `mpqediten64/godot/sim` 和 `data/` 以目录联接挂进这个工程。

验收一律走游戏窗口里的界面：按钮、标签、场景上能看见的状态。不用无头脚本当通过条件。阶段 0 的窗口是 `godot_war3` 里的 `legion_ui/legion_boot.tscn`，界面上能读到开局资源，点「造兵 / 点王 / 出售」后标签跟着变。

---

## 3. 模拟器里已经有的，以及仍是原型的

下面以 `mpqediten64/godot/sim` 为准。阶段 3 不要把已经落地的规则再做一遍。

**已经按策划案落地**

- 开局 300 金 / 114 木（`TickConfig`）。
- 国王强化：80 木、+3 永久收入，生命 / 攻击 / 恢复分开加（`commands.gd` `_king`，数值在 `data/king.txt`）。
- 雇怪：准备阶段入队，战斗开始刷到对方，并按 `hires.txt` 加收入。
- 出售：本回合建造的单位退 100% 金币，旧单位退 50%。
- 兵力：普通单位 = 造价金币（`Economy.force_of`）。这与 KK 官方入门攻略一致，实现时就用这个定义。英雄兵力 = 等级 × 25，英雄单位还没做。
- 农场人口取在场农场的 `food_cap` 最大值，升级是替换。
- 攻防倍率读 `data/armor.txt`。
- `waves.txt` 有 30 行，并带 `force` 列，供出售保护读取。这张表是自定曲线，不是 5.34c 原表。

**原型参数，不能写成 5.34c 原规则**

| 常量 | 现值 | 含义 |
|------|------|------|
| `PREP_TICKS` | 300 | 准备 15 秒。原图准备时长未还原 |
| `MAX_WAVE` | 10 | 测试局长度。回放常见 30–33 波 |
| `START_INCOME` | 5 | 开局收入未在明文给出 |
| `START_FOOD` | 8 | 策划案写主城提供 7 人口。差 1，阶段 3 改成 7，并单独测一局 |
| `START_KING_HP` | 2000 | 国王初始血量未还原 |
| `cell_xy` | 1280×720 上的 6×4 | 像素格，不是魔兽坐标 |
| 玩家数 | 2 | 各有一条国王血 |

国王回复打了两次：`CombatSim` 每 20 拍（1 秒）加一次，回合结算再加一次。阶段 3 收成战斗里这一处。

木材目前只有开局的 114，没有任何采集。没有伐木，雇怪和点王在第一波之后就会停。

---

## 4. 对局结构（先定死再写代码）

明文是 10 个建造席：左路 4 人 + 1 电脑，右路 4 人 + 1 电脑。胜利条件是打死对方国王（每边一座）。每位玩家有自己的国王三围科技和自己的收入。

实现约定：

- **一座阵营国王。** 左路、右路各一个国王实体。5 名建造者的生命 / 攻击 / 恢复加成累加到这座国王上。+3 收入只进购买者自己的账。
- **漏怪打本阵营国王。** 任意一条路走到终点，扣的是这一边的国王，不是某一位玩家的私人国王。
- **电脑席是建造者**，不是国王的替身。国王是地图上的单位。电脑操作阶段 4 再用 `aipool` 对照站位，第一版可以空置或走现有 `cpu.gd`。
- **可玩切片按这个顺序加席：** 保持现有 1v1（两条路、两座国王）直到坐标和伐木验收通过；然后 2v2；最后 4v4 加两个电脑席。1v1 测试文件不改成 8 人。

建造区格子和行军折线是两份数据。格子回答「这个兵放在哪」。折线回答「系统怪和雇兵沿哪条路走到国王」。`get_pathing_map` 只表示格子能不能走，不提供行军路线。

雇怪档位按策划案 5.4：280 木以下随时可雇；约 280–400 木在第 10 波之后；500 木及以上在第 15 波之后。

预言：本局从该族抽出可造单位（公开攻略写的是 6 个，次数与抽取个数做成表，默认 6 个、最多预言 5 次）。第一次不耗木，之后按策划案 5.5 递增。开局不给整族科技树。

英雄：100 金、12 人口，从祭坛造，不占普通兵的升级链。兵力 = 等级 × 25。

---

## 5. 坐标一次改完

现在怪沿像素 X 轴移动，走到 `x<=48` 或 `x>=752` 打国王，射程除以 `RNG_SCALE`（4），每拍走 2 像素。魔兽地图大约宽一万，射程是 100–800。只改 `cell_xy` 会让格子在地图上、怪仍按像素飞。

同一次提交里改这些，并在游戏窗口里把金币、出售返还和国王血重新对上界面上的数字：

- 建造格：`席位 × cell → WC3 XY`，写在 `data/cells.txt`。数量先 6×4；和路径图对不上时改 `CELLS_PER_SIDE`，以空地为准。农场格单独标。
- 每条路一条折线：`data/lanes.txt`，点用 WC3 XY。怪和雇兵沿折线走。
- `CREEP_SPEED`、`HIRE_SPEED` 改成每拍的魔兽单位距离。
- 射程用说明里的原始距离，去掉 `RNG_SCALE`。
- 国王判定改为「走到折线终点」。
- 攻击间隔保持 `cd_max = 攻击间隔秒 / 0.05`。说明里的攻击速度是间隔秒，不是每秒攻击次数。

在这批数据写完之前，`cell_xy` 仍用像素默认值，保证没地图时测试能跑。

出生点从 `map-parse` 的 `w3i` 开始点读取。`MapLoader.show_start_locations` 在编辑器里可以看见这些点。`war3map.j` 只是加密脚本入口，里面没有 `DefineStartLocation`。

---

## 6. 3D 视图怎么摆单位

视图挂在 `godot_war3`，每拍做三件事：快照里新的 `eid` 创建模型，已有 `eid` 更新位置和血量，消失的 `eid` 播死亡后移除。命令仍然进 `CommandApplier`。视图不改 `players`。

`add_unit_instance` 要的是编辑器同款条目，不是单独一个 `eid`。适配层自己组字典，至少包括：

- `typeId`：单位四字码
- `position`：`{x, y, z}`，高度用当前高度图采样
- `angle`、`owner`
- `creationNumber`：与 `eid` 一一对应，供 `remove_unit_instance` 使用

`place_units` 在 `MapLoader` 里默认就是 `false`。游戏里保持关闭，单位只来自快照。`add_unit_instance` 会改动态路径；建造是否合法只看格子占用，不查这块路径。

模型经 `tools/asset-convert` 把 MDX 烤成 `.scn`。先转 `data/units.txt` 和 `waves.txt` 里出现的单位。这些模型在地图 MPQ（`Work/`）里，不在魔兽原版资源里。镜头用 `rts_camera.gd`，开局对准本方建造区。界面继续用 LegionP0 的金、木、收入、人口、波次，不接人族 `CommandCard`。

弹道和飘字在模拟器算出伤害之后播放。

---

## 7. 阶段

编号即顺序。前一阶段验收通过再开下一阶段。

### 阶段 0 · 把模拟器挂进 godot_war3

按第 2 节做目录联接。不改规则。

**验收：** 打开 `legion_ui/legion_boot.tscn` 的窗口。界面显示开局 300 金、114 木、收入 5、国王 2000。点造兵、点王、出售后，同一界面上的数字按规则变化。

### 阶段 1 · 地图对位（模拟器仍用像素坐标）

用 `godot_war3/tools/map-parse` 解析军团战争的 `.w3x`。在编辑器里打开地形、装饰物和路径图。本阶段不跑波次，不造兵。

写出两份数据，先不接到 `CombatSim`：

- `data/cells.txt`：每个建造席的格子编号和 WC3 XY。
- `data/lanes.txt`：每条路的折线，终点落在可走格子上，指向本阵营国王。

开始点列表从 `w3i` 导出，和八个建造区、两座国王对照。对不上的点标出来，不手填一份与解析结果平行的坐标表。

**验收**

- 编辑器能打开该图，悬崖、水和路可辨认。
- 点一个建造格，打印的 WC3 坐标与 `cells.txt` 一致。
- 每条折线的终点在路径图的可走格上。

### 阶段 2 · 战斗改到魔兽坐标（仍在阶段 0 的窗口里看）

按第 5 节一次改完速度、射程、国王线和格子。1v1 的两条路先接上。界面上能看到怪沿格子移动，以及这一局的金币和国王血。

**验收：** 在窗口里打完一局，界面上的金币、出售返还、国王血与操作一致。不要求与魔兽原局数值相同，因为出怪表仍是自定曲线。

### 阶段 3 · 3D 只读视图

在阶段 2 的坐标上挂视图。快照每个 `eid` 一个模型。怪的位置在两拍之间插值，插值不写回模拟器。`PathQuery` 只把折线上的点吸到地面。出手与索敌仍由 `CombatSim._attacks` 决定。

**验收**

- 同一套按钮在 2D 界面和 3D 视图里各走一遍，结束时界面上的胜负、金币、国王血一致。
- 准备阶段造一个单位能看见模型；战斗阶段怪沿折线移动并掉血。

### 阶段 4 · 补经济（仍写在 sim）

表现可以借用弹道、飘字、死亡动画。攻防继续读 `armor.txt`，不用 `CombatDamageTable`。

按这个顺序做，每条都有头测试。已经落地的国王 80 木和出售比例只加测试，不重写。

1. **伐木。** 这是本阶段的第一项。小精灵上限 7。采集间隔普通模式 10 秒（200 拍）。1–8 级每个小精灵每次 +1，9 级起 +2。基础每次采集量明文没有，命名为 `LUMBER_BASE`，默认 1，写在 `data/lumber.txt` 头上。小精灵造价以 `unitbalance.slk` 为准（公开攻略写 50 金，导入时核对）。开局 114 木是赠送，不从采集里倒推。
2. **国王回复只留战斗里每秒那一次。** 回合结算不再加回复。
3. **永久收入。** 回合发放等于当前收入，只增不减。雇怪收入列已在表里的，接到发放前的加收入上（雇怪当时已经加过的，不要加第二次）。
4. **农场链。** 用策划案 5.2 的金、木、人口。开局人口改为 7。升级替换建筑，不叠两座。
5. **出售保护。** 第 5 波后，每回合出售旧单位的兵力合计不超过 400，或允许卖掉 1 个单位。当前兵力低于本波 `waves.txt` 的 `force` 的一半时不能卖。第 10 波起不能卖到该 `force` 的 75% 以下。奴隶主、老兵、螯虾、虾皇、鼠群豁免。`force` 来自自定曲线，规则形状按策划案 5.8。
6. **击杀与漏怪的当场金币。** 公式未还原。先做两列自定数：击杀一个系统怪加 `bounty`，漏到国王不加击杀金。写在 `waves.txt`，默认 0，避免假装还原了赏金。
7. **先知竞赛。** 第一条可玩模式。开局抽 6 个可造单位，预言最多 5 次，第一次免费。默认 300 金 / 114 木。
8. **英雄祭坛。** 100 金、12 人口，光环效果先做成策划案 8.5 里能写清数字的那几条（护甲 +4、远程攻击 +28%、每秒回蓝 1.1 等），其余光环后做。
9. **雇兵波次门。** 按第 4 节的 10 波 / 15 波门槛拒绝过早的高价木怪。

四路、2v2、4v4 放在这九条验收之后。扩席时 1v1 窗口原样保留，四路用另一个场景上的界面看。

16 族全量树、变形、六倍怪、暴露速战的积分判胜再往后。族数据用策划案第 16 章，一次接一族，每族至少一条从 1 阶到封顶的升级链能造出来。

**验收：** 在窗口里走完「抽 6 个兵 → 造小精灵并升一次伐木 → 造兵 → 升农场 → 雇一只 80 木怪 → 国王被击破或打满 `MAX_WAVE`」。界面上能读到伐木净增、出售被拒绝，以及国王只按战斗中的回复在涨。

### 阶段 5 · 对拍，以及明确不做

固定随机种子打现有速战 10 波。记录每波怪数量、漏到国王的伤害、建造花费、出售返还、伐木净增、结束时国王血。

和魔兽录像对比时，只对比玩家造了什么、金币收支和国王血的方向。系统怪数量来自 `waves.txt`，对不上原图是预期。模型死亡播完允许比模拟器晚半秒。

六倍怪只采用明文写死的特例：14 波后雇怪收入减半向下取整；第 20 波 5 只、第 21 波 4 只。其余波次仍用自定曲线。这两条在阶段 4 之后单开模式，不塞进默认局。

不做：

- 反编译或移植 `war3map.bin` / JASS。
- DzAPI、商城、DzFrame、自定义平台 UI。
- 策划案第 10 章的外观指令和赞助权限。
- 在 `godot_war3` 里写第二套血量、冷却或收入。
- 把人族采矿、建造加速、训练队列、英雄技能接到这张图上。
- 中央通道、加防区积分、暴露速战记分板。漏怪在本方案里只表示「走到折线终点并扣国王血」。

---

## 8. 数据从哪来

| 数据 | 来源 | 实现时怎么用 |
|------|------|----------------|
| 攻防克制 | 策划案第 4 章，已在 `armor.txt` | `CombatSim` 的唯一倍率表 |
| 开局资源 | 策划案 5.7 | 按模式写入 `TickConfig`。开局收入 5 保持为原型常量 |
| 农场、主城人口 | 策划案 5.2 | 开局人口 7；农场 `food_cap` 按表 |
| 造兵造价与战斗面板 | `unitbalance.slk` 的金木人口；HP / 攻击用单位说明，不用 SLK 里占位的 500 | 导入工具生成 txt |
| 雇怪木价、收入、解锁波 | 策划案 5.4 | `hires.txt` 增加 `income` 和 `min_wave` |
| 国王三围 | 已在 `king.txt` | 只修回复结算点 |
| 出售保护 | 策划案 5.8 | 阈值读 `waves.txt` 的 `force` |
| 兵力 | 造价金币；英雄等级 × 25 | 保持 `force_of`，英雄另算 |
| 伐木 | 策划案 5.1 的间隔和每级加成 | `LUMBER_BASE` 自定，默认 1 |
| 击杀赏金 | 未还原 | `waves.txt` 的 `bounty`，默认 0 |
| 系统怪曲线 | 未还原 | 继续用现有 `waves.txt`，文件头注明自定 |
| 技能 | 策划案 11.1 先做；11.2 不做 | 见第 9 节 |
| 出生点、格子、路线 | `map-parse` 的 w3i 与路径图 | `cells.txt`、`lanes.txt` |
| 电脑操作 | `aipool/*.json` 是回放 | 只对照站位，不当行为树 |

导入放在 `mpqediten64/godot/tools/`，沿用已经能读 SLK `F;Y` / `F;X` 表头的解析，输出仍是 CSV，让 `GameTables.load_all` 多几列即可。不要另写一份只认 `C;Y` 的解析器。

---

## 9. 技能

技能不写进人族 `AbilityBehaviorCatalog`，也不在 `combat.gd` 里一条一个函数。

11.1 里能对上数字的，用一张效果表，效果种类先只做这四种：持续伤害、减速、光环加减、死亡时治疗或伤害。乌头毒素、蛛网、淬毒标枪、甲胄、西里诺克斯之优雅、治愈之叶、禁忌之果走这张表。数值用策划案 11.1 的 Dur / Data，不从 `war3map.bin` 猜。

11.2（跃迁、梦魇、鼠王随数量缩放、按波数反弹的荆棘）留空，单位说明里照样显示名字。

---

## 10. 不接的 godot_war3 模块

| 模块 | 路径 | 原因 |
|------|------|------|
| 对战开局 | `game/scripts/logic/melee_bootstrap.gd` | 会在出生点放主城和农民 |
| 采集 | `game/scripts/logic/economy/` | 金矿进出、伐木打树，和回合发钱、伐木科技不是同一套 |
| 人族建造 | `game/scripts/logic/construction/` | 农民工地、修理加速 |
| 训练与科技研究 | `game/scripts/logic/production/`、`tech/tech_presence.gd` | 兵营训步兵、顶盾研究 |
| 人族技能表 | `game/scripts/data/ability_behavior_catalog.gd` | 只注册了水元素、暴风雪等 |
| 单位 AI | `game/scripts/logic/ai/unit_ai.gd` | 会抢 `CombatSim` 的目标 |
| 伤害管线 | `game/scripts/logic/combat/damage_pipeline.gd` | 原版攻防表。飘字可以接，扣血不接 |
| 编辑器笔刷 | `godot_war3/editor/` | 阶段 1 用来看图，不进对局 |

可以接的只有：`MapLoader.reload_from_hf`、`get_pathing_map`（吸地）、`add_unit_instance` / `remove_unit_instance`、模型场景、RTS 相机、弹道与飘字的播放、选中环。

---

## 11. 阶段 0–3 结束时要有的文件

```text
mpqediten64/godot/          LegionP0 规则脚本，由 godot_war3 的界面引用
  sim/                      现有规则；阶段 2 改坐标，阶段 4 加伐木
  data/
    cells.txt               阶段 1，席位与格子 → WC3 XY
    lanes.txt               阶段 1，行军折线
    lumber.txt              阶段 4，LUMBER_BASE 与每级加成
    armor.txt               已有
    waves.txt               已有，自定曲线；阶段 4 补 bounty
    king.txt                已有
  tools/                    沿用现有 SLK 导入

Work/godot_war3/
  legion_sim/                 联接到 mpqediten64/godot/sim
  legion_data/                联接到 mpqediten64/godot/data
  legion_ui/
    legion_boot.tscn          阶段 0 起的验收窗口
    legion_boot.gd
```

`GameWorld.cell_xy` 在 `cells.txt` 就绪并由阶段 2 打开开关之后才改读它。开关关闭时仍用像素格。
