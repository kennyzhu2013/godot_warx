# 基于 godot_war3 实现军团战争

> 目标：在 `godot_war3` 里做出可玩的《军团战争 TD》5.34c。镜头、底栏、模型、寻路、攻击和扣血都用这套引擎里已经在跑的魔兽对战。军团规则是 `GameDirector` 上的一种对局模式。
> 数值以同目录 [军团战争TD_5.34c_策划案.md](军团战争TD_5.34c_策划案.md) 为准。出怪表、基础伐木量、击杀赏金在策划案里未还原，实现时写成命名常量，文件头标明「自定规格」。
> 本文只定实现边界和阶段，不改游戏代码。

---

## 1. 结论

只做一个 Godot 工程：`Work\godot_war3`。主场景仍是 `game/scenes/game_main.tscn`。地图换成已解析的军团战争图 `res://assets/map-parsed/legiontd`。玩家看见的是现有 `GameHud`：金、木、人口、肖像、命令格、小地图。

权威状态留在引擎已有的对象上：

| 状态 | 权威 |
|------|------|
| 金、木、人口 | `PlayerStock`，`GameHud.bind_stock` 只读刷新 |
| 场上单位、国王 | `MapLoader.add_unit_instance` 摆出的单位，生命在单位自己的生命组件上 |
| 移动 | `UnitNavigator` + `PathQuery`，沿 `lanes.txt` 的魔兽坐标折线下移动命令 |
| 出手、扣血、死亡 | `AttackController` → `DamagePipeline` → `DeathService`。飘字和弹道走现有 Present |
| 命令 | `CommandCard` + `CommandRouter`。造兵、点王、出售、雇怪是命令格上的动作 |

原图脚本在加密的 `war3map.bin` 里。本工程没有 JASS / Lua 宿主，不把 `.w3x` 丢进场景指望它自己跑。`map-parse` 已经把地形、路径、区域和开始点写成 `assets/map-parsed/legiontd`。玩法按策划案写在对局模式里，调上面这些接口。

人族 Melee 开局不跑：`MeleeBootstrap` 不会在出生点放主城和五个人类农民，也不会把库存设成 500 金 / 150 木。军团开局写进 `PlayerStock`：300 金、114 木、人口上限 7。

```text
策划案 + legion_data/*.txt + map-parsed/legiontd
        │
        ▼
GameDirector 上的军团对局模式
        │
        ├── GameSession / PlayerStock     金木人口收入
        ├── CommandCard / CommandRouter   造兵、雇怪、点王、出售
        ├── MapLoader                     地形、摆单位、拆单位
        ├── UnitNavigator / PathQuery     沿折线走、吸地
        ├── AttackController
        │     DamagePipeline
        │     DeathService                攻击、扣血、死亡
        └── GameHud                       底栏只读这些状态
```

---

## 2. 工程怎么接

| 角色 | 路径 |
|------|------|
| 对战工程 | `d:\game2\rpg\mpqediten64\Work\godot_war3` |
| 主场景 | `game/scenes/game_main.tscn` |
| 地图 | `assets/map-parsed/legiontd` |
| 军团表 | `legion_data/`（`armor.txt`、`units.txt`、`hires.txt`、`waves.txt`、`king.txt`、`cells.txt`、`lanes.txt`、`starts.txt`） |
| 本文与策划案 | `Work\godot\`，不进工程 |

`game_main.tscn` 里 `MapRoot.map_dir` 从 Echo Isles 改为 `res://assets/map-parsed/legiontd`。`place_units` 保持关闭：`war3mapUnits.doo` 单位数是 0，场上单位只由对局模式生成。镜头用现有 `rts_camera.gd`，开局 `focus_on_position` 对准本方建造区。

验收只看游戏窗口：底栏数字、命令格、场上模型和血条。打开的就是 `game_main`，不另做一套标签窗口。

---

## 3. 规则写在哪

下列规则都还没接到这张图上。实现时加在 `game/scripts/` 的军团对局模式里，调用第 1 节的现成接口。数值表放在 `legion_data/`。

**按策划案要落地的**

- 开局 300 金 / 114 木，写入 `PlayerStock`。开局收入 5 是原型常量，策划案没有明文。
- 国王强化：80 木、+3 永久收入。生命、攻击、恢复分开加，数值读 `king.txt`。花木走 `PlayerStock.try_spend`，属性加在国王单位上。
- 雇怪：准备阶段入队，战斗开始用 `add_unit_instance` 刷到对方路上，并按 `hires.txt` 给自己加收入。
- 出售：本回合建造的单位退 100% 金币，旧单位退 50%。退款走 `PlayerStock.add_gold`，单位走 `remove_unit_instance`。
- 兵力：普通单位 = 造价金币。英雄兵力 = 等级 × 25。英雄单位后做。
- 农场人口取在场农场的 `food_cap` 最大值，升级是替换同一格建筑，并改 `PlayerStock.food_cap`。
- 攻防倍率读 `legion_data/armor.txt`，装进本局使用的伤害表，仍由 `DamagePipeline` 扣血。
- `waves.txt` 有 30 行，并带 `force` 列，供出售保护读取。这张表是自定曲线，不是 5.34c 原表。

**先写成命名常量，文件头标明自定规格**

| 常量 | 取值 | 含义 |
|------|------|------|
| 准备时长 | 15 秒 | 原图准备时长未还原 |
| 测试局波数 | 10 | 回放常见 30–33 波。打通后再拉到表里的 30 行 |
| 开局收入 | 5 | 明文未给出 |
| 开局人口上限 | 7 | 策划案写主城提供 7 |
| 国王初始生命 | 2000 | 明文未还原，写在 `king.txt` |
| 玩家数 | 先 1v1 | 两条路、两座国王。扩席见第 4 节 |

国王回复只在战斗中每秒加一次，加在国王单位的生命上，封顶为最大生命。回合结算不再加第二次。

木材目前表上只有开局的 114。伐木是第 4 阶段的第一项：小精灵是场上单位，到点给 `PlayerStock.add_lumber`。没有这一项时，雇怪和点王在第一波之后就会停。

---

## 4. 对局结构（先定死再写代码）

明文是 10 个建造席：左路 4 人 + 1 电脑，右路 4 人 + 1 电脑。胜利条件是打死对方国王（每边一座）。每位玩家有自己的国王三围科技和自己的收入。

实现约定：

- **一座阵营国王。** 左路、右路各一个国王单位。5 名建造者的生命 / 攻击 / 恢复加成累加到这座国王上。+3 收入只进购买者自己的 `PlayerStock`。
- **漏怪打本阵营国王。** 怪走到折线终点后，由 `AttackController` 打这座国王。扣的是国王的生命，不是某一位玩家的私人血条。
- **电脑席是建造者**，不是国王的替身。国王是地图上的单位。电脑席第一版空置；后做时只通过 `CommandRouter` 下和玩家相同的命令。站位对照 `Work/aipool` 的回放，回放不是行为树。
- **可玩切片按这个顺序加席：** 1v1（两条路、两座国王）直到坐标和伐木在窗口里验收通过；然后 2v2；最后 4v4 加两个电脑席。1v1 场景不改成 8 人。

建造区格子和行军折线是两份数据。格子回答「这个兵放在哪」。折线回答「系统怪和雇兵沿哪条路走到国王」。`get_pathing_map` 只表示格子能不能走，不提供行军路线。

雇怪档位按策划案 5.4：280 木以下随时可雇；约 280–400 木在第 10 波之后；500 木及以上在第 15 波之后。

预言：本局从该族抽出可造单位（公开攻略写的是 6 个，次数与抽取个数做成表，默认 6 个、最多预言 5 次）。第一次不耗木，之后按策划案 5.5 递增。开局不给整族科技树。抽到的单位显示在 `CommandCard` 上。

英雄：100 金、12 人口，从祭坛造，不占普通兵的升级链。兵力 = 等级 × 25。祭坛出兵走现有训练队列的同一套「花钱、占人口、生成单位」，单位四字码和造价读军团表。

---

## 5. 坐标

地图大约宽一万，射程是 100–800。建造格、行军和射程从第一版就用魔兽坐标。

- 建造格：`席位 × cell → WC3 XY`，已在 `legion_data/cells.txt`。和路径图对不上时改格子划分，以可走、可造的空地为准。农场格单独标。
- 每条路一条折线：`legion_data/lanes.txt`，点用 WC3 XY。怪和雇兵由 `UnitNavigator` 沿折线走，`PathQuery` 把点吸到地面。
- 移动速度用单位说明里的魔兽移速，由导航按秒积分。
- 射程用说明里的原始距离，交给 `CombatQuery` 的出手射程。
- 国王判定是「走到折线终点」，然后对国王下攻击命令。
- 攻击间隔用武器表里的间隔秒。`AttackController` 的冷却已经按秒跑，说明里的攻击速度是间隔秒，不是每秒攻击次数。

出生点从 `map-parse` 的 `w3i` 读取，对照写在 `starts.txt`。`MapLoader.show_start_locations` 在编辑器里可以看见这些点。`war3map.j` 只是加密脚本入口，里面没有 `DefineStartLocation`。

---

## 6. 单位和界面

对局模式在三个时机动单位：准备阶段玩家造出的兵、战斗开始刷出的系统怪和雇兵、死亡或出售时移除。位置和生命就在单位节点上，导航和攻击控制器每帧自己更新。

`add_unit_instance` 要编辑器同款条目：

- `typeId`：单位四字码
- `position`：`{x, y, z}`，高度用当前高度图采样
- `angle`、`owner`
- `creationNumber`：供 `remove_unit_instance` 使用

建造是否合法只看格子占用和 `get_pathing_map` 的可造标记。造兵命令不走人类农民的工地和建造加速。

模型经 `tools/asset-convert` 把 MDX 烤成 `.scn`。先转 `units.txt` 和 `waves.txt` 里出现的单位。这些模型在地图 MPQ（`Work/`）里，不在魔兽原版资源里。底栏继续用 `GameHud` 和 `CommandCard`。收入和当前波次加在现有资源条旁，仍由 `PlayerStock` 和对局时钟驱动。

弹道和飘字由 `DamagePipeline.damage_applied` 和 `ProjectileService` 播放，和人族对战同一条线。

---

## 7. 阶段

编号即顺序。前一阶段在游戏窗口里验收通过，再开下一阶段。

### 阶段 0 · 用军团地图开一局

`game_main` 加载 `legiontd`。跳过 `MeleeBootstrap`。本地 `PlayerStock` 设为 300 金、114 木、人口 0/7。相机对准本方建造区。底栏显示这三组数。

**验收：** 窗口里是军团地图的地形，顶栏是 300 金、114 木、人口 0/7。没有人族主城，没有五个农民，也不是 Echo Isles 的 500 金 / 150 木。

### 阶段 1 · 地图对位

地形、装饰物和路径图已经在 `assets/map-parsed/legiontd`。本阶段不刷波、不造兵。

`cells.txt`、`lanes.txt`、`starts.txt` 已经从 `war3map.w3r` / `war3map.wpm` / `war3map.w3i` 写出。在 3D 窗口里点建造格和折线终点，用底栏状态行打出 WC3 坐标。

**验收**

- 游戏窗口里悬崖、水和路可辨认。
- 点一个建造格，状态行坐标与 `cells.txt` 一致。
- 每条折线的终点落在路径图的可走格上。

### 阶段 2 · 造兵、走路、国王

1v1，两条路、两座国王。命令格提供「造当前兵」「强化国王生命」「出售」。造兵在格子上立刻出现模型，并扣 `PlayerStock`。系统怪沿折线走到国王面前。国王血条就是该单位的生命。

**验收：** 在窗口里造一个兵、点一次国王、卖掉刚造的兵。底栏金币和国王血条与这三次操作一致。怪能沿路走到国王附近。出怪数量仍是自定曲线。

### 阶段 3 · 战斗走现有伤害管线

怪和己方兵用 `AttackController` 索敌、按射程出手。`DamagePipeline` 用本局装入的 `armor.txt` 倍率扣 `UnitLife`。死亡走 `DeathService`，播死亡动画后 `remove_unit_instance`。国王每秒回复只加在战斗阶段。

**验收：** 窗口里能看见攻击、掉血飘字和死亡。漏到终点的怪在打国王，国王血条下降。出售返还和底栏金币一致。

### 阶段 4 · 经济与军团命令

表现继续用现有弹道、飘字、死亡动画。下面每条都在游戏窗口里单独能看出来。

1. **伐木。** 第一项。小精灵是单位，上限 7。普通模式每 10 秒结算一次。1–8 级每个小精灵每次 +1，9 级起 +2。基础每次采集量明文没有，命名为 `LUMBER_BASE`，默认 1，写在 `legion_data/lumber.txt` 头上。造价以 `unitbalance.slk` 为准（公开攻略写 50 金，导入时核对）。开局 114 木是赠送。结算调用 `PlayerStock.add_lumber`。
2. **国王回复只留战斗中每秒一次。**
3. **永久收入。** 回合发放等于当前收入，只增不减。发放走 `PlayerStock.add_gold`。雇怪收入列在雇的当时已经加过的，发放时不要再加一次。
4. **农场链。** 用策划案 5.2 的金、木、人口。开局人口上限 7。升级替换该格建筑，并改 `food_cap`。
5. **出售保护。** 第 5 波后，每回合出售旧单位的兵力合计不超过 400，或允许卖掉 1 个单位。当前兵力低于本波 `waves.txt` 的 `force` 的一半时不能卖。第 10 波起不能卖到该 `force` 的 75% 以下。奴隶主、老兵、螯虾、虾皇、鼠群豁免。`force` 来自自定曲线，规则形状按策划案 5.8。拒绝时底栏状态行写出原因，金币不变。
6. **击杀与漏怪的当场金币。** 公式未还原。`waves.txt` 增加 `bounty`，默认 0。击杀系统怪时 `add_gold(bounty)`，漏到国王不加这笔。
7. **先知竞赛。** 第一条可玩模式。开局抽 6 个可造单位，放进命令格。预言最多 5 次，第一次免费。默认 300 金 / 114 木。
8. **英雄祭坛。** 100 金、12 人口。光环先做策划案 8.5 里能写清数字的几条（护甲 +4、远程攻击 +28%、每秒回蓝 1.1 等），挂到现有 Buff。其余光环后做。
9. **雇兵波次门。** 按第 4 节的 10 波 / 15 波门槛，命令格拒绝过早的高价木怪，并在状态行说明。

四路、2v2、4v4 放在这九条验收之后。扩席时 1v1 场景保留，四路用同一个 `game_main` 换座位数据。

16 族全量树、变形、六倍怪、暴露速战的积分判胜再往后。族数据用策划案第 16 章，一次接一族，每族至少一条从 1 阶到封顶的升级链能在命令格里造出来。

**验收：** 在窗口里走完「抽 6 个兵 → 造小精灵并升一次伐木 → 造兵 → 升农场 → 雇一只 80 木怪 → 国王被击破或打满测试局波数」。底栏能看到伐木后木材变多、非法出售被拒绝，以及国王血条只在战斗中按回复上涨。

### 阶段 5 · 打一局，以及明确不做

在窗口里打完固定的 10 波。记下每波怪数量、漏到国王的伤害、建造花费、出售返还、伐木净增、结束时国王生命。这些数都从底栏和国王血条读。

和魔兽录像对比时，只对比玩家造了什么、金币收支和国王血的方向。系统怪数量来自 `waves.txt`，和原图不同是预期。

六倍怪只采用明文写死的特例：14 波后雇怪收入减半向下取整；第 20 波 5 只、第 21 波 4 只。其余波次仍用自定曲线。这两条在阶段 4 之后单开模式，不塞进默认局。

不做：

- 反编译或移植 `war3map.bin` / JASS。
- DzAPI、商城、DzFrame、自定义平台 UI。
- 策划案第 10 章的外观指令和赞助权限。
- 另一套血量、冷却、收入或底栏。军团局只写 `PlayerStock`、单位生命和 `GameHud`。
- 人族采矿循环、农民工地、兵营训练队列、人类英雄技能接到这张图的开局上。
- 中央通道、加防区积分、暴露速战记分板。漏怪是「走到折线终点并攻击国王」。

---

## 8. 数据从哪来

| 数据 | 来源 | 实现时怎么用 |
|------|------|----------------|
| 攻防克制 | 策划案第 4 章，`legion_data/armor.txt` | 本局伤害表，`DamagePipeline` 读取 |
| 开局资源 | 策划案 5.7 | 写入 `PlayerStock`。开局收入 5 保持为原型常量 |
| 农场、主城人口 | 策划案 5.2 | 开局 `food_cap = 7`；农场按表改 `food_cap` |
| 造兵造价与战斗面板 | `unitbalance.slk` 的金木人口；HP / 攻击用单位说明，不用 SLK 里占位的 500 | 导入进 `units.txt`，命令和 `UnitBalance` 读这张表 |
| 雇怪木价、收入、解锁波 | 策划案 5.4 | `hires.txt` 的 `income`、`min_wave` |
| 国王三围 | `king.txt` | 点王命令改国王单位属性 |
| 出售保护 | 策划案 5.8 | 阈值读 `waves.txt` 的 `force` |
| 兵力 | 造价金币；英雄等级 × 25 | 出售保护用，显示可加在底栏 |
| 伐木 | 策划案 5.1 的间隔和每级加成 | `LUMBER_BASE` 自定，默认 1 |
| 击杀赏金 | 未还原 | `waves.txt` 的 `bounty`，默认 0 |
| 系统怪曲线 | 未还原 | 继续用现有 `waves.txt`，文件头注明自定 |
| 技能 | 策划案 11.1 先做；11.2 不做 | 见第 9 节 |
| 出生点、格子、路线 | `map-parse` 的 w3i、w3r、wpm | `starts.txt`、`cells.txt`、`lanes.txt` |
| 电脑操作 | `Work/aipool` 回放 | 只对照站位 |

SLK 导入放在 `godot_war3/tools/`，沿用已经能读 `F;Y` / `F;X` 表头的解析，多出来的列写入 `legion_data` 的表。

---

## 9. 技能

11.1 里能对上数字的，加成一张效果表，挂到现有 Buff / 技能路径上，由 `DamagePipeline` 和 `BuffHost` 结算。效果种类先只做四种：持续伤害、减速、光环加减、死亡时治疗或伤害。乌头毒素、蛛网、淬毒标枪、甲胄、西里诺克斯之优雅、治愈之叶、禁忌之果走这张表。数值用策划案 11.1 的 Dur / Data。

11.2（跃迁、梦魇、鼠王随数量缩放、按波数反弹的荆棘）留空。单位说明里照样显示名字。

人族已经接好的暴风雪、水元素留在人族对战里。军团局的命令格只列出本局抽到的单位、雇怪、点王、出售和上面这张效果表里的技能。

---

## 10. 这张图用什么、不用什么

**用这些**

| 模块 | 作用 |
|------|------|
| `game_main.tscn` / `GameDirector` | 对局壳和模式开关 |
| `GameSession` / `PlayerStock` | 玩家与金木人口 |
| `GameHud` / `CommandCard` | 底栏和命令 |
| `MapLoader` | 地形、`add_unit_instance` / `remove_unit_instance`、`get_pathing_map` |
| `UnitNavigator` / `PathQuery` / `rts_camera.gd` | 走路、吸地、镜头 |
| `CommandRouter` | 命令入口 |
| `AttackController` / `CombatQuery` | 索敌和出手 |
| `DamagePipeline` / `DeathService` | 唯一扣血和死亡 |
| `ProjectileService` / 飘字 | 攻击表现 |
| 选中环、血条 | 现有 Present |

**这张图的开局不调用**

| 模块 | 原因 |
|------|------|
| `MeleeBootstrap` | 会在出生点放主城和农民，并把库存设成 500 / 150 |
| 人族 `HarvestController` 的金矿循环 | 军团木材是小精灵定时结算，金不从金矿搬 |
| 人族 `BuildSite` 工地 | 军团造兵是格子上立即生成，不派农民修建 |
| 人族兵营训练和顶盾科技 | 命令格的内容和人族科技树不同 |
| `editor/` 笔刷 | 看图可以开编辑器，笔刷不进对局 |

人族这些模块留在 Echo Isles 对战里，不删。军团模式只是 `GameDirector` 的另一条开局：换地图、换库存、换命令格、换刷怪。

---

## 11. 阶段 0 结束时要有的接法

```text
Work/godot_war3/
  game/scenes/game_main.tscn     地图改为 map-parsed/legiontd
  game/scripts/                  军团对局模式加在这里，调用现有 Session / 命令 / 战斗
  legion_data/                   规则表与格子、折线
  assets/map-parsed/legiontd/    地形、路径、区域、开始点
```

阶段 0 只改开局地图和 `PlayerStock`。造兵、折线移动和伤害从阶段 2、阶段 3 再接到命令格和 `DamagePipeline`。
